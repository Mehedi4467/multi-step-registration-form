import Spinner from '@/components/custom/Common/Spinner';
import React from 'react';

const Loading = () => {
    return (
        <div>
            <Spinner/>
        </div>
    );
};

export default Loading;